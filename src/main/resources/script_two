SELECT NAME, MAX(COUNT_PERS) AS COUNT_P FROM COMPANY
INNER JOIN (SELECT COMPANY_ID AS COMP, COUNT(COMPANY_ID) AS COUNT_PERS
FROM PERSON
GROUP BY COMP) AS RES ON ID = COUNT_PERS
GROUP BY NAME, COUNT_PERS
ORDER BY COUNT_P DESC LIMIT 1